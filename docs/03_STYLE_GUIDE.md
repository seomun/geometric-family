# 03. 스타일 가이드 v7.2 「굿즈 에디션」 — 일관성 규격 (잠금)

작성 2026-09-18. 기준 이미지: `notes/snapshots/` 최신 `characters_*.png`. **이 문서와 `src/characters.js` 가 어긋나면 코드가 틀린 것이다.**
목표물: 노트·가방·스티커·이모티콘. 헬로키티 문법 — 어디에 찍어도, 몇 년 뒤에도 같은 얼굴.

## 1. 절대 규칙 (바꾸면 v7)
| 항목 | 값 |
|---|---|
| 외곽선 색 | INK `#6b5443` **연갈색. 검정·진갈색 금지** (2026-09-18 결정: 진한 테두리는 무겁고 클립아트 느낌) |
| 외곽선 굵기 | `LINE = 2.6` (2026-09-18 레퍼런스 계측: 가는 선이 더 세련됨). 눈·눈썹·세부선은 0.4~0.8배, 세모 아내 윗눈꺼풀만 1.35배 |
| 채색 | **플랫 단색.** 그라데이션·광택·그림자·질감 필터 없음 |
| 볼터치 | `#f2a1ad` 55%, 눈 바깥 아래 |
| 눈 | **크게**: 흰자 rx 10.5 ry 12 + INK 동공 6.2 + 하이라이트 2개(큰 것 좌상 2.6, 작은 것 우하 1.3). 위치 = 얼굴 중심 −1, 간격 ±13.5 (오밀조밀) |
| 얼굴 크기 | 몸 폭 100 기준 스케일 1 × 1.18 (내부 고정). 키티 비율 — 얼굴이 몸의 대부분 |
| 팔다리 | **가늘게**: 팔 폭 0.085u, 다리 폭 0.095u, 손 0.075u, 발 0.085u(몸색 35% 어둡게). 외곽선 = LINE×1.6 추가 |
| 다리 | 몸 아래 0.13u. **각선미**: 무릎 살짝 바깥, 발목 안쪽으로 모이는 곡선 |
| 입 | 작게(폭 12), 눈 바로 아래 15. 키티처럼 이목구비가 가운데 모인다 |
| 종이색 | PAPER `#f4ecdd` · 상복 SUIT `#4a4a52` |

## 1-1. 여성 캐릭터 규격 (세모 아내·네모 엄마·할머니·큰딸·동그라미 엄마·딸) — 각선미가 핵심
| 항목 | 값 |
|---|---|
| 다리 | `shapedLimb`: 허벅지 0.095u → 종아리 볼록 +0.034u(t=0.58) → 발목 0.036u. 무릎 바깥 0.09u, 발목 안쪽으로 모임. 다리 길이 +0.05u |
| 팔 | 위팔 0.068u → 살짝 볼록(t=0.32, +0.018u) → 손목 0.03u. 직선 금지, 항상 곡선 |
| 손 | 반지름 0.058u (남성 0.075u) |
| 구두 | 펌프스(`heel`): 발색 45% 어둡게, 발 방향 좌/우 대칭 |
| 의상(`outfit`) | `dress`(얇은 스캘럽 밑단 + 작은 리본) · `blouse`(흰 깃 + 리본) · `skirt`(주름 밴드) · `apron`(네모 엄마 기본) · `cardigan`(깃 + 진주, 동그라미 엄마·할머니 기본) · `pearl` · `none`. 색은 `outfitColor` |
| 기본 의상 | 세모 아내 dress(흰) · 네모 엄마 apron(크림) · 할머니 cardigan(베이지) · 큰딸(`girl:true`) skirt(분홍) · 동그라미 엄마 cardigan(회베이지)+진주 · 동그라미 딸 skirt |
| 얼굴 | 속눈썹 3개, 분홍 입술(세모 아내), 리본(세모 아내 apex 우측·동그라미 딸 핀) |
시트: `web/women.html` (팔다리 확대 ×2.6 포함). 남성은 `limb`(균일 굵기) 유지 — 남녀 실루엣 차이가 곧 식별자다.

## 1-2. 렌더 모드 — clean(원본) / hand(손 터치) — "AI 느낌" 제거 장치
캐릭터 벡터는 항상 clean 으로 유지한다(굿즈·인쇄·이모티콘 원본). **웹툰·쇼츠·블로그 삽입 컷은 `GF.setMode('hand')`** 로 렌더한다.
hand 가 하는 일(전부 SVG 필터, 원본 무수정):
| 효과 | 값 | 왜 |
|---|---|---|
| 선 흔들림 | feTurbulence 0.016 + displacement 2.2 | 완벽한 곡선 = 클립아트 느낌 |
| 선 굵기 변화 | dilate 0.5 를 노이즈 마스크로 부분 적용 | 붓·펜 압력 느낌 |
| 칠 어긋남 | 칠 패스를 (0.8, 0.5) 이동 + 저주파 흔들림 2.0 | 리소/실크 인쇄의 판 어긋남 |
| 종이 결 | `paper(w,h)` 오버레이, multiply 9% | 화면이 종이가 된다 |
| 미세 기울기 | 캐릭터마다 −1.5°~+1.5° (입력 길이 해시, 결정적) | 좌우 완벽 대칭 제거 |
비교 시트: `web/touch.html`. 값은 여기서만 바꾼다. **이모티콘·굿즈 벡터 납품은 clean.**
트렌드 근거: 2025~26 캐릭터 = "불완전한 매력"(흐트러진 선, 낮은 채도 파스텔, 빈틈) — 망그러진 곰·최고심·빵빵이 계열.

## 1-3. 세모 부부 = 래스터 정본 (2026-09-23, D4 실행)
- 정본 시트: `assets/wife/_sheet/sheet_v1.png` (ChatGPT, 2×3). 잘린 낱장: `assets/wife/{good,joy,wink,love,surprise,worry}.png` (600×600 투명, 발끝 하단 5% 정렬).
- 남편도 동일: `assets/husband/` (시트 v1, 6표정). 상복 컷만 코드 벡터 유지.
- 코드는 `semoWife({emo})` / `semoHusband({emo})` 호출 시 자동으로 해당 PNG 배치(`GF.sprite`). 표정은 `sheetEmo(who, emo)` 가 **캐릭터별로 있는 칸 중 가장 가까운 것**을 고른다(예: angry → bad → worry → good).
- 아내 시트 v2(2026-09-23): angry/bad/smug/tired/cry/calm 추가 → 전투 장면 해결.
- 반입은 `assets/_inbox/<이름>.png` + `python tools/ingest.py` (규칙표는 docs/06 상단).
- **래스터 적용 캐릭터(2026-09-23)**: 세모 아내(12표정) · 세모 남편(6) · 네모 아빠(6) · **동그라미 아빠(6: good/joy/calm/warm/surprise/trouble)**. 나머지는 코드 벡터 유지.
- 동그라미 가족은 **표정 폴백이 다르다**(`EMO_FALLBACK_CALM`): angry·bad·sad 모두 `calm`/`trouble` 로 수렴한다 — "약해 보이지 않는다" 원칙의 코드 구현.
- 코드 벡터 아내는 `semoWife({ raster: false })` 로 남아 있다(대안·비교용). 전역 전환: `GF.setRasterWife(false)`.
- 새 표정/포즈는 `_sheet/sheet_v1.png` 를 첨부해 같은 스타일로 생성 → `tools/cut_sheet.py` (docs/05).
- 남은 차이: 래스터는 포즈가 고정(팔 내림)이라 `pose` 가 무시된다. 포즈가 필요한 컷은 시트 v2(포즈 3×3) 를 뽑는다.

## 2. 캐릭터 고유 식별자 (하나라도 빠지면 그 캐릭터가 아니다)
| 캐릭터 | 색 | 식별자 |
|---|---|---|
| 네모 아빠 | `#e29368` | 숱 적은 머리 3가닥 · 콧수염(입 위) · 눈밑주름 · 모서리 r=0.16w |
| 네모 엄마 | `#ecb383` | 쪽머리(오른쪽 위 점) · 속눈썹 · **손 4개** |
| 네모 할머니 | `#d8b58e` | 흰 쪽머리(정수리) · 눈밑주름 |
| 네모 아이들 | `#f0c583` `#f2b49e` `#e6a9c0` `#f8dcb0`(막둥이) | 정수리 삐침 · 큰 눈(big) · 얼굴 0.72배 |
| 세모 남편 | `#9fb6d0` | **정삼각형 ▲**(변 살짝 볼록) · 둥근 꼭짓점 · 퀴프 · 아몬드 눈. 남▲ 여▽ — 마주 서면 마름모 |
| **세모 아내** | `#ecc76a` | **순수 역삼각형 ▽** (모서리만 둥글게, 변 직선, **실루엣·의상 없음** — 2026-09-18 결정). 매력은 얼굴에서만: `wifeFace` — 눈 크게·낮게(0.4h)·넓게, 바깥꼬리 살짝 위, 홍채+하이라이트 3, 두꺼운 윗눈꺼풀, 바깥쪽 속눈썹 3, 눈썹 얇고 멀리, 입 아주 작게, 볼터치 높게, **왼쪽 모서리 큰 리본**, 앞머리 두 가닥. **v7 정본 = faceStyle `ref`** (2026-09-18 ChatGPT 레퍼런스 `notes/ref/wife_ref_2026-09-18.png` 를 코드로 복제): 몸 `#f6de5c` 밝은 노랑, 모서리 r=0.11, **팔다리 살색 `#f7dcc8`**(몸색 아님) 아주 가늘게, 팔은 몸 옆으로 얌전히(손 ±0.46u, 0.3u), 다리 곧게 모아(무릎 0.035u), **분홍 펌프스 `#f08aa4`**, **큰 리본 k=0.1size 가 왼쪽 위 모서리를 덮음**(고리 2·매듭·꼬리 2), 눈 = 계측값: 눈 가로:세로 1.7 (rx 12.2·ry 7.2), 눈 간격 ±17.5, 눈 높이 0.44h, 홍채 rx 8.8·ry 8.6(눈보다 커서 잘림, #5a3722), 동공 4.8, 하이라이트 좌상 2.3 + 우하 0.9, **윗눈꺼풀 선 1.35배 굵기 — 안쪽 1/4 지점이 가장 높고 바깥 끝은 처짐(요염)**, 바깥 속눈썹 3(길이 5.6~7.4, 휨), 아래 눈꺼풀 짧게, **기본 시선은 왼쪽으로 0.9 (애교)**, 눈썹 얇은 짧은 아치, 볼 진분홍 원 r=4.2, 입 아주 작은 미소. 앞머리 없음. 대안: chic / shoujo / kitty / dot. 시트 `web/faces.html` |
| 동그라미 아빠 | `#9fb0bf` | 가르마 · 안경 · **눈매 고정(calm = 둥근 눈 + 윗눈꺼풀 선)**. 표정은 입으로만 |
| 동그라미 엄마 | `#c4b3a0` | 웨이브 앞머리 · 속눈썹 · 눈매 고정 |
| 동그라미 아들/딸 | `#a9bcae` / `#cbb6c2` | 바이올린 / 테니스 라켓 + 분홍 핀 |

## 3. 표정 12 · 포즈 10 · 소품 10
- emo: `good joy bad angry worry relief sad cry surprise love tired wink`. 동그라미는 눈매 고정 — 입·눈썹(angry 만)·땀으로만 표현. "약해 보이지 않는다."
- pose: `stand wave cheer think point hips shrug hold sit walk`. `hold`·`think` 는 팔이 몸 앞. `point` 는 gaze 방향.
- item(18): `coffee americano phone envelope book bag violin racket heart star sword shield` + **중년 소품** `glasses2`(돋보기) `pills`(약봉지) `tie`(넥타이) `hat`(등산모) `condolence`(경조사 봉투) `remote`(리모컨). `item` 은 오른손, `itemL` 은 왼손.
- **나이는 얼굴이 아니라 소품·대사·배경으로 낸다** (D5, 백서 §5). 어른 얼굴을 늙게 고치지 않는다.
- 구도 함수: `coupleBattle(x,y,size)` 마주보고 칼·방패 / `coupleLove(x,y,size)` 하트 / `semoWifePeek({x,y,size,edgeY})` 담 너머 엿보기(팔이 담을 안음). 굿즈 아트보드 `web/goods.html`.
- 새 표정/포즈/소품은 **여기에 먼저 적고** 코드에 추가한다. 시트(`web/characters.html`)에 자동으로 나타나야 한다.

## 3-1. 역할 (누가 어디의 얼굴인가)
- **세모(특히 아내)** = 매력. 굿즈·스티커·이모티콘·쇼츠 썸네일의 주역. 표정·포즈를 가장 많이 만든다.
- **네모** = 평범. 화자. 블로그의 이름이자 목소리. 웃는 얼굴 기본.
- **동그라미** = 완벽·균형. 표정 적어도 된다. 로고·브랜드 마크·'정답' 자리.
- 전체 기본값은 **웃는 얼굴**. 슬픔·피곤은 이야기 안에서만.

## 4. 금지
그라데이션, 외곽선 색 변경, 눈 모양 변경, 몸 비율 변경(네모 w:h ≈ 1.13), 캐릭터별 다른 선 굵기, AI 이미지로 대체, 손그림 필터.

## 5. 검증 절차
1. `node tools/snapshot.js` → `notes/snapshots/<날짜>_characters.png`, `_ep01.png`.
2. 직전 스냅샷과 나란히 놓고 본다. 의도한 변화 외에 달라진 곳이 있으면 되돌린다.
3. 변경은 DEVLOG 에 "v6.x: 무엇을 왜" 한 줄.

## 6. 굿즈 규격 메모
- 노트 표지: 캐릭터 1 + 한 줄 문구(Gaegu 700) + 작은 로고. 배경은 캐릭터 색의 밝은 변주.
- 스티커: 24~28px 반지름 기준 9종 표정. 재단선 4px 안쪽.
- 이모티콘(카카오 기준 24종): 표정 12 × 포즈 조합에서 뽑는다. 텍스트 없이 동작만으로 읽혀야 한다.
